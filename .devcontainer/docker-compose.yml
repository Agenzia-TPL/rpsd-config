include:
  - ./docker-compose-database.yml
  - ./docker-compose-keycloak.yml

services:
  rpsd-config:
    extends:
      file: ./docker-compose-base.yml
      service: rpsd-config
    depends_on:
      - postgis
      - keycloak-db
      - keycloak
